pandas
numpy 
seaborn
matplotlib
scikit-learn
catboost
xgboost
dill
Flask
# -e .
# -e . is used to automatically run setup.py (which created an application as a package). Comment it out after the first run of pip install -r requirements.txt because the whole setup.py will run each time 
# you use requirements.txt